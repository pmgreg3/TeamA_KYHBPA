@model KYHBPA_TeamA.Models.PartnerOrgViewModel

@{
    Layout = null;
}

<div id="carousel" class="carousel slide" data-ride="carousel" data-interval="2000" 
     data-pause="hover" data-wrap="true">
    <!-- Menu -->
    <ol class="carousel-indicators">
        <li data-target="#carousel" data-slide-to="0" class="active"></li>
        <li data-target="#carousel" data-slide-to="1"></li>
        <li data-target="#carousel" data-slide-to="2"></li>

    </ol>

    <!-- Items -->
    <div class="carousel-inner">
        <div class="item-active">
            <div class="row">
                @*@for (int i = 0; i < Model.Partners.Count; i++)
                {

                    if (i <= 3)
                    {*@
                        <div class="col-xs-3">
                            <img src="@Url.Action("GetPhoto", "Photo", new { Model.Partners.ElementAt(0).Id })" />
                        </div>
                <div class="col-xs-3">
                    <img src="@Url.Action("GetPhoto", "Photo", new { Model.Partners.ElementAt(1).Id })" />
                </div>
                <div class="col-xs-3">
    <img src="@Url.Action("GetPhoto", "Photo", new { Model.Partners.ElementAt(2).Id })" />
</div>
                <div class="col-xs-3">
    <img src="@Url.Action("GetPhoto", "Photo", new { Model.Partners.ElementAt(3).Id })" />
</div>
                    @*}
                }*@
            </div>
        </div>
        <div class="item">
            <div class="row">
                @*@for (int i = 4; i < Model.Partners.Count; i++)
                {

                    if (i > 3 && i < 7)
                    {*@
                        <div class="col-xs-3">
                            <img src="@Url.Action("GetPhoto", "Photo", new { Model.Partners.ElementAt(4).Id })" />
                        </div>
                <div class="col-xs-3">
                    <img src="@Url.Action("GetPhoto", "Photo", new { Model.Partners.ElementAt(5).Id })" />
                </div>
                <div class="col-xs-3">
                    <img src="@Url.Action("GetPhoto", "Photo", new { Model.Partners.ElementAt(6).Id })" />
                </div>
                    @*}
                }*@
            </div>
        </div>
    </div>
    <a href="#carousel" class="left carousel-control" data-slide="prev">
        <span class="glyphicon glyphicon-chevron-left"></span>
    </a>
    <a href="#carousel" class="right carousel-control" data-slide="next">
        <span class="glyphicon glyphicon-chevron-right"></span>
    </a>
</div>

